#include <stdio.h>

/**
 * main - main is the entry point of the program
 *
 * Return: returns zero
 */

int main(void)
{
	int i;

	for (i = 0; i < 10; i++)
		printf("%d", i);
	printf("\n");
	return (0);
}
